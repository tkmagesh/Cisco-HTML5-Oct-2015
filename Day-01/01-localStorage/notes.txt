window.localStorage
    - key-value store
    - both key and value should be strings
    - 10 MB size limit
    - Same Origin Policy
    
    - APIs
        * setItem(key, value)
        * getItem(key) => value
        * removeItem(key)
        * key(index) => key at index
        * clear()
        * length
        
        * storage event